<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <h3 class="nav-item">
          <% if user_signed_in? %>
            <%= "¡Saludos! #{current_user.email}" %>
          <% else %>
            <a href="<%= user_session_path %>"></a>
          <%end%>
        </h3>
      </li>

      <% if user_signed_in? %>
        <span class="navbar-item active">
          <h3>
            Tareas Realizadas:
            <%= current_user.pending_tasks.where(done: true).count%>
            -
            <%= current_user.pending_tasks.count %>
          </h3>
        <% end %>
        <li class="nav-item">
          <a class="nav-link disabled" href="<%= destroy_user_session_path%>" ,="," data-method='DELETE'>Log out</a>
        </li>
      </ul>

    </div>
  </nav>
